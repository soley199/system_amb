
$(document).on('click', '#btnEnviarEncuesta360', function() {
    var recursos = document.getElementById("tipoencu360Encu").value;
    

    if(recursos == "RRHH"){
      if ($('input:radio[name=optionsRadios1]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 1 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios2]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 2 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios3]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 3 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios4]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 4 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios5]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 5 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios6]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 6 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios7]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 7 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios8]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 8 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios9]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 9 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios10]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 10 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios11]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 11 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios12]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 12 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios13]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 13 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios14]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 14 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios15]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 15 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios16]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 16 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios17]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 17 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios18]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 18 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios19]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 19 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios20]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 20 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios21]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 21 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios22]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 22 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios23]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 23 Esta vacía');
        return false;
      } if (document.getElementById("option24").value == "") {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 24 Esta vacía');
        return false;
      }if ($('input:radio[name=optionsRadios25]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 25 Esta vacía');
        return false;
      }if ($('input:radio[name=optionsRadios26]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 36 Esta vacía');
        return false;
      }if ($('input:radio[name=optionsRadios27]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 27 Esta vacía');
        return false;
      }if ($('input:radio[name=optionsRadios28]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 28 Esta vacía');
        return false;
      }else{
        $("#modalEnviarEncu360").submit();
        // document.getElementById("modalEnviarEncu360").submit();
        // alert("Terminada");
      }

    }else{
      if ($('input:radio[name=optionsRadios1]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 1 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios2]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 2 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios3]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 3 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios4]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 4 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios5]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 5 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios6]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 6 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios7]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 7 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios8]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 8 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios9]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 9 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios10]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 10 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios11]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 11 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios12]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 12 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios13]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 13 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios14]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 14 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios15]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 15 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios16]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 16 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios17]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 17 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios18]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 18 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios19]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 19 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios20]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 20 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios21]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 21 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios22]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 22 Esta vacía');
        return false;
      } else if ($('input:radio[name=optionsRadios23]:checked').val() == null) {
        // Si no se cumple la condicion...
        alert('[ERROR] La pregunta 23 Esta vacía');
        return false;
      } else{
        $("#modalEnviarEncu360").submit();
        // document.getElementById("modalEnviarEncu360").submit();
        // alert("Terminada");
      }
    }
   

  });


  $(document).on('click', '#btn360jefe', function() {
    var Numtarjeta360 = $(this).attr("Num_Tarjeta360");
    var tipoencu360 = $(this).attr("tipoencu360");
    var contra360 = $(this).attr("contra360");

    $("#num_TerjetaCon").val(Numtarjeta360);
    $("#tipoencu360Con").val(tipoencu360);
    $("#contra360Con").val(contra360);

    // alert('Tarjeta '+Numtarjeta360+' quien '+tipoencu360);
  });
  $(document).on('click', '#btn360auto', function() {
    var Numtarjeta360 = $(this).attr("Num_Tarjeta360");
    var tipoencu360 = $(this).attr("tipoencu360");
    var contra360 = $(this).attr("contra360");

    $("#num_TerjetaCon").val(Numtarjeta360);
    $("#tipoencu360Con").val(tipoencu360);
    $("#contra360Con").val(contra360);

    // alert('Tarjeta '+Numtarjeta360+'quien '+tipoencu360);
  });
  $(document).on('click', '#btn360compa1', function() {
    var Numtarjeta360 = $(this).attr("Num_Tarjeta360");
    var tipoencu360 = $(this).attr("tipoencu360");
    var contra360 = $(this).attr("contra360");

    $("#num_TerjetaCon").val(Numtarjeta360);
    $("#tipoencu360Con").val(tipoencu360);
    $("#contra360Con").val(contra360);

    // alert('Tarjeta '+Numtarjeta360+'quien '+tipoencu360);
  });
  $(document).on('click', '#btn360compa2', function() {
    var Numtarjeta360 = $(this).attr("Num_Tarjeta360");
    var tipoencu360 = $(this).attr("tipoencu360");
    var contra360 = $(this).attr("contra360");

    $("#num_TerjetaCon").val(Numtarjeta360);
    $("#tipoencu360Con").val(tipoencu360);
    $("#contra360Con").val(contra360);

    // alert('Tarjeta '+Numtarjeta360+'quien '+tipoencu360);
  });
  $(document).on('click', '#btn360RRHH', function() {
    var Numtarjeta360 = $(this).attr("Num_Tarjeta360");
    var tipoencu360 = $(this).attr("tipoencu360");
    var contra360 = $(this).attr("contra360");

    $("#num_TerjetaCon").val(Numtarjeta360);
    $("#tipoencu360Con").val(tipoencu360);
    $("#contra360Con").val(contra360);

    // alert('Tarjeta '+Numtarjeta360+'quien '+tipoencu360);
  });
  
  $(document).on('click', '#btnContinuarEncu360', function() {

    passEncu360User = $("#continuarEncu360Pass").val();
    num_TerjetaCon = $("#num_TerjetaCon").val();
    tipoencu360Con = $("#tipoencu360Con").val();
    contra360Con = $("#contra360Con").val();



if(tipoencu360Con == "RRHH"){
  if (passEncu360User == contra360Con) {

    $('#modalContinuarEncuesta360').modal('toggle');
    $("#nuevoFacturaMaterialRuta").val(factura);
    $('#modalEncuesta360').modal('show'); 
    $("#num_TerjetaEncu").val(num_TerjetaCon);
    $("#tipoencu360Encu").val(tipoencu360Con);

    $('input[name=option24]').attr("disabled", false);
    $('input[name=optionsRadios25]').attr("disabled", false);
    $('input[name=optionsRadios26]').attr("disabled", false);
    $('input[name=optionsRadios27]').attr("disabled", false);
    $('input[name=optionsRadios28]').attr("disabled", false);

    // $("#nuevoIdProveedorMaterialRuta").val("");
    // $("#nuevoProveedorMaterialRuta").val("");
    // $("#datepicker").val("");

   } else {
    alert("Contraseña Incorrecta");
    //  $('#btnAgregarOrdenCompra').attr("disabled", true);
    //  $('#btnGuardarFactura').attr("disabled", true);
     // $("#datepicker").datepickerEditaFechaFacturaer("option", "dateFormat", $(this).val());
     // console.log(new Date("YYYY-MM-DD"));
   }

}else{
  if (passEncu360User == contra360Con) {

    $('#modalContinuarEncuesta360').modal('toggle');
    $("#nuevoFacturaMaterialRuta").val(factura);
    $('#modalEncuesta360').modal('show');
    $("#num_TerjetaEncu").val(num_TerjetaCon);
    $("#tipoencu360Encu").val(tipoencu360Con);

    // $("#nuevoIdProveedorMaterialRuta").val("");
    // $("#nuevoProveedorMaterialRuta").val("");
    // $("#datepicker").val("");

   } else {
    alert("Contraseña Incorrecta");
    //  $('#btnAgregarOrdenCompra').attr("disabled", true);
    //  $('#btnGuardarFactura').attr("disabled", true);
     // $("#datepicker").datepickerEditaFechaFacturaer("option", "dateFormat", $(this).val());
     // console.log(new Date("YYYY-MM-DD"));
   }

}


    

   
  });